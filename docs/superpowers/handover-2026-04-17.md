# OpenDNA — Handover Note, 2026-04-17

**Current at end of day 2026-04-17.** Orients a future Claude Code session or collaborator picking up the repo cold.

## What this is

Local-first web utility that parses a raw 23andMe-style DNA file, matches against 8 curated SNP panels, joins findings with ClinVar + PharmGKB/CPIC annotations, and renders a self-contained HTML report. Optional BYOK LLM synthesis (Anthropic default with prompt caching, OpenAI secondary) adds prose interpretation.

- **Repo:** https://github.com/corbett3000/OpenDNA — **public**, Apache 2.0
- **Runs at:** http://localhost:8787 (default)
- **Not published to PyPI yet** — install from source

## Status at handover

**MVP is live and public.** Built and shipped in one day via a 12-task TDD plan (see `docs/superpowers/plans/2026-04-17-opendna-mvp.md` — still in repo for design rationale, even though git history was squashed to one commit).

### Shipped features

- 8 curated SNP panels (cardiovascular, methylation, pharmacogenomics, athletic, dietary, HFE, cognition, stimulant sensitivity) — ~50 SNPs, each with ≥2 genotype interpretations.
- ClinVar (14 rsids) + PharmGKB (7 rsids, CPIC drug recs for TPMT / CYP2C19 / CYP2C9 / VKORC1 / SLCO1B1) shipped subsets.
- FastAPI server with five endpoints: `/`, `/api/panels`, `/api/analyze`, `/api/analyze-stream` (SSE for live progress), `/api/update-db`.
- Vanilla-JS SPA: DOM APIs only (no `innerHTML` on dynamic data), report rendered into sandboxed iframe. Live progress bar with per-stage messages. Optional browser `localStorage` persistence of form values behind a *Remember* checkbox + *Clear saved values* button.
- CLI: `opendna serve`, `opendna scan <file>`, `opendna update-db`.
- Jinja2 HTML report (autoescape on) styled after the original sandbox synthesis — stat tiles, color-coded card grid per panel, critical-findings alert box for risk tier, AI synthesis block when LLM is configured.
- LLM provider layer: pluggable ABC, Anthropic default (`claude-sonnet-4-6` + prompt caching), OpenAI (`gpt-4o` using `max_completion_tokens`). API keys held per-request in memory only.
- Analyzer normalizes allele order (`AG == GA`) AND reverse complement (panel `CC` matches file `GG` for non-palindromic C/T SNPs).
- 44 tests passing (parser, panels, analyzer, annotations, report, LLM providers, server, CLI, SSE stream, e2e). `ruff check` clean. CI runs on Python 3.11 / 3.12 / 3.13.
- Apache 2.0 license.
- README with full setup guide, per-provider raw-DNA download instructions, sample report + screenshot, troubleshooting, author links (`@corbett3000`, `stillrush.co/bio`).
- Sample report: `examples/sample-report.html` (served live via raw.githack; preview image at `docs/assets/sample-report.png`).

### Open threads

1. **Live upstream fetcher** — `scripts/build_annotations.py` is a stub that prints what it would do. The full ClinVar VCF streamer + PharmGKB API client lands in v0.1.1.
2. **Final whole-project code review** not yet run.
3. **Not on PyPI** — install is git-clone-only for now. Release to PyPI whenever the user is ready (tag `v0.1.0` + add a PyPI publish workflow to CI).
4. **v0.2:** chat-with-your-genome pane, PDF export.
5. **v0.3:** VCF / WGS input.
6. **v0.4:** polygenic risk scores with population adjustment.

### Known environment quirk

- Python 3.11 not installed locally on this Mac; 3.13 used instead. Project declares `>=3.11` and the classifiers cover 3.11–3.13, so local 3.13 is fine. CI still tests 3.11 on ubuntu-latest runners.
- `uvloop` / `httptools` 3.13 wheels may briefly lag upstream releases — if CI goes red on the 3.13 leg, wait or drop 3.13 from the matrix.

## Git history note

Commit history was **squashed to a single "Initial public release" commit on 2026-04-17**. The dev timeline is no longer in `git log` — the design spec (`docs/superpowers/specs/2026-04-17-opendna-design.md`) and implementation plan (`docs/superpowers/plans/2026-04-17-opendna-mvp.md`) are preserved in-tree for provenance. A local backup tag `backup-pre-squash-2026-04-17` pins the pre-squash tip on the user's machine only.

## How a future session picks this up

If Claude Code opens this repo, `CLAUDE.md` at the root will auto-load with a condensed primer including load-bearing invariants and user preferences. Start there, then:

```bash
cd ~/Coding/OpenDNA
source .venv/bin/activate          # or create one: python3 -m venv .venv && pip install -e ".[dev,all]"
pytest -v                          # 44 tests should pass
opendna serve                      # localhost:8787
```

## Key design decisions (for context)

| Decision | Choice | Why |
|---|---|---|
| Backend | FastAPI + Uvicorn | Python-native, strong JSON + SSE support |
| Frontend | Vanilla JS, no build step | Zero contribution friction, no npm |
| Privacy | API keys per-request only, never persisted server-side | Trust claim verifiable by reading ~50 LOC |
| Annotations | Shipped subsets + `update-db` refresh path | Instant first-run, explicit freshness control |
| LLM | Optional, pluggable, BYOK | Rule-based works with no key |
| Default model | `claude-sonnet-4-6` + prompt caching | Matches user's tooling; caching cuts cost |
| License | Apache 2.0 | Patent grant + common in bio/data tooling |
| Port | 8787 | Unused by common dev stacks |
| Browser storage | `localStorage`, opt-in | Convenience without disk writes |

## Load-bearing invariants (repeated here for durability)

1. Raw DNA file NEVER leaves the machine under any configuration.
2. LLM calls send only filtered findings (~40-50 lines of structured text), never raw DNA.
3. API keys are held in memory for one request, never persisted, redacted in logs.
4. Server binds to 127.0.0.1 by default; `--host 0.0.0.0` requires explicit opt-in.
5. Jinja autoescape is on and must stay on — XSS-escape test pins this.
6. No `innerHTML` with dynamic content in the SPA; report renders into a sandboxed iframe.

## User preferences (sticky)

- Terse, direct responses — no trailing recaps.
- No `Co-Authored-By: Claude` trailers in commit messages.
- Privacy claims in docs must match reality — no overclaiming.
- When a major change lands, restart the local dev server so the browser reflects it.

## Session transcript

The original MVP build session ran from `~/Coding/sandbox/`, not this repo. Raw transcript (if still present) lives under `~/.claude/projects/-Users-corbett3000-Coding-sandbox/`. Resuming from this repo's directory starts a fresh Claude Code session that will use `CLAUDE.md` and this handover note as orientation.
